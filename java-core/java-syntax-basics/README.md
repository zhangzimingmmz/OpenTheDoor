# Java 语法基础 (Java Syntax Basics)

> 掌握 Java 语言的基础语法，包括数据类型、运算符、控制流、异常处理、字符串和数组

## 📚 模块内容

### 1. 数据类型 (Data Types)

#### 基本数据类型
- **PrimitiveTypesDemo** - 8种基本数据类型演示
  - 整型：byte, short, int, long
  - 浮点型：float, double
  - 字符型：char
  - 布尔型：boolean
  - 类型转换：自动转换和强制转换
  - 溢出问题

#### 包装类
- **WrapperClassDemo** - 包装类使用演示
  - 自动装箱和拆箱
  - 包装类常用方法
  - 缓存机制（-128~127）
  - 注意事项

### 2. 运算符 (Operators)

- **OperatorsDemo** - 运算符使用演示
  - 算术运算符：+, -, *, /, %, ++, --
  - 关系运算符：==, !=, >, <, >=, <=
  - 逻辑运算符：&&, ||, !
  - 位运算符：&, |, ^, ~, <<, >>, >>>
  - 赋值运算符：=, +=, -=, *=, /=
  - 其他运算符：?: (三元), instanceof
  - 运算符优先级

### 3. 控制流 (Control Flow)

#### 条件语句
- **ConditionalDemo** - 条件控制语句演示
  - if 语句
  - if-else 语句
  - if-else if-else 语句
  - 嵌套 if 语句
  - switch 语句（传统）
  - switch 表达式（Java 14+）

#### 循环语句
- **LoopDemo** - 循环控制语句演示
  - for 循环
  - 增强 for 循环（for-each）
  - while 循环
  - do-while 循环
  - 嵌套循环
  - break 和 continue
  - 标签（Labels）

### 4. 异常处理 (Exception Handling)

- **ExceptionDemo** - 异常处理演示
  - 异常的分类（Checked vs Unchecked）
  - try-catch 基础
  - 多个 catch 块
  - finally 块
  - throw 和 throws
  - try-with-resources (Java 7+)
  - 自定义异常
  - 异常链

### 5. 字符串处理 (String Handling)

- **StringDemo** - 字符串处理演示
  - String 基础
  - String 常用方法（查找、替换、分割等）
  - String 不可变性
  - StringBuilder 和 StringBuffer
  - 字符串比较（equals vs ==）
  - 字符串实战操作

### 6. 数组 (Arrays)

- **ArrayDemo** - 数组使用演示
  - 数组声明和初始化
  - 数组遍历
  - 数组操作（查找、排序、复制）
  - 多维数组
  - Arrays 工具类
  - 数组实战操作

## 🚀 快速开始

### 运行示例

所有示例都包含 main 方法，可以直接运行。

```bash
# 编译项目
mvn clean compile

# 运行特定示例（在 IDE 中直接运行）
# 例如：PrimitiveTypesDemo.java
```

### 包结构

```
cn.openthedoor.java.syntax
├── datatypes/          # 数据类型
│   ├── PrimitiveTypesDemo.java
│   └── WrapperClassDemo.java
├── operators/          # 运算符
│   └── OperatorsDemo.java
├── controlflow/        # 控制流
│   ├── ConditionalDemo.java
│   └── LoopDemo.java
├── exception/          # 异常处理
│   └── ExceptionDemo.java
├── string/             # 字符串
│   └── StringDemo.java
└── array/              # 数组
    └── ArrayDemo.java
```

## 💡 学习要点

### 重点知识
1. **数据类型**
   - 8种基本类型的范围和默认值
   - 自动装箱/拆箱机制
   - 包装类缓存范围

2. **运算符**
   - 位运算的实际应用
   - 短路运算 vs 非短路运算
   - 运算符优先级

3. **控制流**
   - switch 新旧语法对比
   - break 和 continue 的使用场景
   - 标签在嵌套循环中的应用

4. **异常处理**
   - Checked 和 Unchecked 异常的区别
   - try-with-resources 的优势
   - 异常链的构建

5. **字符串**
   - String 不可变性的原理
   - String vs StringBuilder vs StringBuffer
   - 字符串常量池机制

6. **数组**
   - 数组是引用类型
   - Arrays 工具类的常用方法
   - 多维数组的本质

### 易错点 ⚠️

1. **基本类型 vs 包装类**
   ```java
   Integer a = 128;
   Integer b = 128;
   System.out.println(a == b); // false（超出缓存范围）
   System.out.println(a.equals(b)); // true
   ```

2. **字符串比较**
   ```java
   String s1 = "Java";
   String s2 = new String("Java");
   System.out.println(s1 == s2); // false
   System.out.println(s1.equals(s2)); // true
   ```

3. **for-each 不能修改数组**
   ```java
   int[] arr = {1, 2, 3};
   for (int num : arr) {
       num = num * 2; // 不会改变数组
   }
   ```

4. **switch 的 fall-through**
   ```java
   switch (x) {
       case 1:
           System.out.println("1");
           // 忘记break会继续执行case 2
       case 2:
           System.out.println("2");
           break;
   }
   ```

## 🎯 面试高频问题

### 1. == 和 equals() 的区别？
- `==` 比较的是引用（地址）
- `equals()` 比较的是内容
- 基本类型只能用 ==

### 2. String、StringBuilder、StringBuffer 的区别？
- String：不可变，线程安全
- StringBuilder：可变，非线程安全，性能高
- StringBuffer：可变，线程安全，性能较低

### 3. 什么是自动装箱和拆箱？
- 自动装箱：基本类型 → 包装类（Integer i = 10）
- 自动拆箱：包装类 → 基本类型（int n = i）
- 注意空指针异常

### 4. Checked 和 Unchecked 异常的区别？
- Checked：编译时必须处理（IOException）
- Unchecked：运行时异常（NullPointerException）

### 5. finally 块一定会执行吗？
- 几乎总是执行
- 特殊情况：System.exit()、JVM 崩溃、无限循环

### 6. 数组和集合的区别？
- 数组：固定长度，可以存基本类型
- 集合：长度可变，只能存对象

## 📖 扩展学习

### 推荐阅读
- 《Java核心技术》第1卷 - 第3-5章
- 《Effective Java》- 第8-10章
- Oracle Java Tutorials - Language Basics

### 相关知识
- [Java 面向对象编程](../java-oop) - OOP 三大特性
- [Java 集合框架](../java-collections) - ArrayList、HashMap 等
- [Java 新版本特性](../java-new-features) - Switch 表达式、Text Blocks

## ✅ 练习建议

1. **基础练习**
   - 编写程序计算1到100的和
   - 判断一个数是否为质数
   - 反转字符串

2. **进阶练习**
   - 实现冒泡排序
   - 实现二分查找
   - 异常处理的最佳实践

3. **实战练习**
   - 学生成绩管理系统
   - 计算器程序
   - 文件读取异常处理

---

**返回：** [Java Core 主页](../)  
**下一章：** [Java 面向对象编程 →](../java-oop)

